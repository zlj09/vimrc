export QT_X11_NO_MITSHM=1

alias ctags_all='ctags -R --c++-kinds=+p+l+x+c+d+e+f+g+m+n+s+t+u+v --fields=+liaS --extra=+q --language-force=c++'

alias unzip_gz="gzip -d"
alias unzip_tar_gz="tar -xvzf" 
alias unzip_tgz="tar -xvzf"
alias unzip_tar="tar -xvf"

alias zip_tar_gz="tar -czvf"

alias lt="ls -ltra --time-style=full-is" 

alias vim="vimx"

alias rclone_mount_onedrive_gatech="rclone --vfs-cache-mode writes mount onedrive_gatech: ~/cloud/onedrive_gatech &"

PATH=$PATH:/home/gtcad9/lzhu308/software/anaconda3/bin

# alias ds="du -h -a --max-depth=0 * | sort -rh" 
# alias dh="du -h -a --max-depth=1" 
alias ds="du -ahd0 * | sort -rh" 
alias dh="du -ahd1" 

# cp_exclude [relative path to directory to exclude] /path/from /path/to
alias cp_exclude="rsync -ax --exclude"

# Check my disk storage space
alias dm="df -h | grep 'lzhu308\|gtcad9'"

# Clear auxiliary files generated by Innovus, Tempus, Voltus, etc.
alias spnr_clear="rm -rf .cadence innovus.* voltus.*  .lzhu308_launch_* .*slk.gz"
# rm -rf .cadence .template alib-52 checkDesign extLogDir *via_layer* *.log* *.cmd* default.svf *.rpt* *warning *res.net scheduling* *.bin model.asrt* design.* dynamic_* static_* timing* voltus* work transcript vish_* .qor_* all_pblock.tcl .powerAnalysis* decap.cmd die_model_* package_model/ RLRP
